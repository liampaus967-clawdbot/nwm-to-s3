# AWS S3 Configuration (uses AWS profile, no keys needed)
S3_BUCKET_NAME=fgp-flow-percentile-data-west-dev
AWS_REGION=us-east-1
AWS_PROFILE=fgp

# Database (for COMID list - optional, not currently used)
DATABASE_URL=postgresql://river_router:password@host:5432/river_router
